apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hive-environment.fullname" . }}-test-connection"
  labels:
  {{- include "hive-environment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "hive-environment.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
